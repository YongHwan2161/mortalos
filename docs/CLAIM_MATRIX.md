# MortalOS current claim matrix

Status: **ACTIVE — EXACT-MAIN VERIFY/RELEASE/DEPLOY PASS AT `a6cfb657…`; TLS-CHANNEL-BOUND CRASH-RECOVERABLE ROLE-KEY-ATTESTED COORDINATOR-NON-AUTHORITY ALL-ROLE-KEYS-RATIFIED PUBLIC-CHAIN; EXTERNAL ISSUER HONESTY, SYBIL, ARBITRARY-NAT REACHABILITY, AND PHYSICAL INDEPENDENCE HOLD**

Last synchronized: **2026-08-30 KST**

The latest promoted release is source
`a6cfb657212a0b0c20848851d86786b62b9e14f7`, tree
`bf111d3314359a2646f282b7ce28475f04c5c410`. Exact-main Verify
`33052134872/1` completed `protocol`, `browser-parity`, and
`Promote exact release candidate` successfully with zero job annotations. Artifact
`9643090168` has GitHub digest
`sha256:8801a6ea0f116691c8c62ffc66545eab29b6ff40f1d8cdebbe413ed0d23ab524`.
Exact `workflow_run` Deploy `33064417517/1` completed successfully. Two no-cache
public-manifest reads were byte-identical at 1,266 bytes, named that source commit,
and all nine public assets (1,346,187 bytes total) matched their declared SHA-256
values under asset-set digest
`sha256:EPCDV6PCFMjMIfpMdLU2Q9zmR_5ptz9wpsl35YvykCE`.

That promotion covers the named immutable release only. This documentation successor
requires its own exact-head policy/CI, immutable review, and expected-head merge; it
does not promote any physical, administrative, issuer-honesty, arbitrary-NAT, or
global-availability claim.

Topology clarification: the current public chain uses native-direct TLS `/2`; explicit
reverse-proxy identity-only `/1` compatibility is separate. Both fixtures still share
one PC and one remote address; neither is multi-host or independent-administration
evidence.

This matrix is the current claim boundary. “Implemented” means code exists in the
named source layer (a candidate row is explicitly labeled);
“exact-source verified” means the named gates passed for that exact source;
“physically verified” requires distinct physical or administrative failure domains;
and “promoted” requires the stage's receipt, review, merge, and post-merge evidence.
No column inherits truth from another.
Governance and deployment are exact-SHA external facts read from the PR, checks,
merge record, post-merge workflows, and deployed manifest. A row never self-promotes
the revision that contains it.

The preceding placement-history candidate completed
128 signed transitions and 381 genuine replacements in `2,841,685.4279ms` test-body
time (`2,842,481.1467ms` runner; `2,842,596ms` shell). Chromium/Lab completed 127
cycles from generation 2 in `2,549,195ms` dynamic time (`2,666,619ms` total). Both
reached generation 129 with 384 distinct provider/lease/chain high-waters
(`128/128/128` by shard) and 387 distinct receipts, then proved a separately signed
generation-130 `3/3` candidate whose plus-one chain commit failed closed without
changing the generation-129 bytes. The Chromium guard is now `3,300,000ms`; workflow
ceilings remain 240 minutes. The historical `7,065.8s` full-suite PASS predates the
ceiling source. A later runtime/test/workflow candidate ran uninterrupted `npm test` from
`2026-08-11 01:06:58.716+09:00` through `03:21:35.542+09:00`, exited `0`, and
completed every ordered stage through final `verify:s4` in `8,076,826ms`
(`8,076.826s`; `134m 36.826s`). That run predates the current WebRTC runtime/test/
security remediation and is historical. That historical candidate's focused gate
covers the combined 512-message/8,388,608-raw-byte transcript ceiling, duplicates,
outbound and inbound atomicity, virtual-transport byte parity, at-most-once native
close capability use, and one close of the still-live peer in the remote-channel
scenario: Node `24/24` in `31,241ms` and actual Chromium in `50,086ms`. On the
merged base, a separate
hidden-wrapper `npm test` ran uninterrupted from
`2026-08-11T06:42:38.6738575+09:00` to
`2026-08-11T09:06:30.4636057+09:00`, exited `0` after `8,631,790ms`
(`143m 51.790s`), and completed final `verify:s4`. Its source/runtime/test/workflow
bytes remained unchanged afterward and the related-workload inventory was zero after
excluding its own probe. The pre-attestation liveness-policy source epoch independently passed
uninterrupted `npm test` in `7,476,222ms` through final `verify:s4`. Those historical
local runs remain provenance; the current release status is the exact-main Verify,
artifact, Deploy, and public readback recorded above.

| Capability | Implemented | Exact-source verified | Physically verified | Promoted | Explicit boundary |
| --- | --- | --- | --- | --- | --- |
| Portable v0 lifecycle and R1 canonical operation/result bytes | Yes | Yes | Not applicable | Yes | Validation authority is the portable kernel, never an adapter or UI. |
| Bounded deterministic `mortalos/1` state transition | Yes | Yes | Not applicable | Yes | This is content binding and deterministic transition, not availability or arbitrary code execution. |
| Read-only canonical evidence export/import | Yes | Yes | Not applicable | Yes | Import does not confer signing authority. |
| A→B browser custody succession after A closes | Yes | Yes, Chromium persistent profiles | No | Yes, within the browser-profile claim | Profiles are not evidence of separate people, devices, organizations, or credential domains. |
| Logical browser `2-of-3` loss and D repair | Yes | Yes, isolated Chromium contexts | No | Yes, as logical quorum evidence | Current contexts can share one host and administrative domain. |
| Effect-time execution, multi-action reconciliation, and successor completion | Yes, internal private-capability slice | Single-shard Node concurrency/retry/commit-then-failure recovery and origin-cut Chromium shard-0 completion pass. Fresh provider and Continuity sessions persist exact requests/results, restore completed effects/commits with `0` underlying calls, and use no-replace owner claims so two-process races create one side effect. Restored single-effect results require exact outer and encoded-placement schemas plus canonical round-trip; their completion slot binds the immutable repair slot, prior commit, manifest, and successor generation while retaining the result ID in candidate content, so a self-rehashed ignored-field variant cannot create a second Continuity call. The repair-hardening executor regression passes `1/1` in `1,272,562.4408ms`; exact-main Verify `33052134872/1` covers the successor in the complete suite. A crashed winner's unresolved claim can be completed only by importing an exact signed placement or verified successor Capsule/commit after the outer executor rederives and verifies it; the recovery capabilities expose no provider execution or Continuity signing method. Invalid proof is rejected before result publication, valid proof restores with `0` duplicate calls, and missing proof remains unavailable. The two-shard Node batch passes `2/2` in `755,752.0478ms`, canonicalizes reversed actions, resumes interrupted providers without repeating prior effects, and converges concurrent calls. Its bounded range adapter verifies canonical frames and deduplicates response payload bytes. The origin-cut Chromium Lab now feeds it an actual connected DataChannel range: an exact duplicate creates no new frame, a rewrapped response deduplicates by payload, late proof after provider 0 leaves calls `[1,0]` providers and `0` Continuity, and mid-batch disconnect leaves the same later-call zeros. The signed-evidence schedule corpus drives the production range session through `10,000 × 8` deterministic events and matches digest `sha256:t0Guc2x3-rrM8G9q7iqYZ1nYNriIj77sgcPort-E5iM` in Node, fresh process, and bundled Chromium with duplicate provider/accounting/Continuity effects `0/0/0`. | No | No | Public plans, relay metadata, self-hashed recovery records, and unsigned local result files remain non-capabilities. Recovery is local-directory-custody scoped and is neither timeout/takeover nor result discovery. The connected pair is same-host/manual signaling, not background gossip, arbitrary NAT reachability, or an independent failure domain. The corpus is a deterministic effect ledger anchored by representative full executor/DataChannel gates, not 10,000 external writes. Independent domains remain HOLD; exact-SHA release promotion is established only for `a6cfb657212a0b0c20848851d86786b62b9e14f7`. |
| Consent-gated durable `1-of-1` participant reload | Yes | Yes, Chromium schema v2 | No | Yes, within exact receipt/deploy evidence | Uses the same replay/WAL path as durable quorum; it remains one-key authority. |
| Unified deterministic Participant Core | Yes | Yes, Node and Chromium | Not applicable | Yes | S1 receipt v2 is main-history portable; live, durable, handoff, quorum, catch-up, fork, snapshot, and availability paths use one core. |
| Crash-safe durable `2-of-3` cold recovery and sign-once journal | Yes, for conforming endpoint concurrency | Yes on merged source `12e90e6…`: Node plus actual Chromium/Firefox bounded parity; historical Chromium `100/100` receipt remains commit-bound | No | Historical S2 claim only; revised claim reopened | Module-private capability and CAS block public-API/store-facade bypass. A same-origin script can still use the persisted non-extractable key outside the journal, so XSS-resistant sign-once is explicitly **HOLD** pending a separate signer trust domain. |
| Exact R3 state replica recovery after storage/relay loss | Yes | Yes, 1 MiB/16-chunk logical stores, 20,000 end-to-end recovery executions, and JavaScript/Python differential | No | Yes, exact-main S3 receipt/review/Verify/Deploy | Inventory is a hint; exact fetched bytes and aggregate root are reverified. Logical stores do not prove independent domains. |
| Confidential replicated state with epoch-key removal | Yes, main | Node plus actual Chromium/Firefox passed on `12e90e6…`; recovery/activation mutation corpus passed | No | Historical receipt only; revised claim reopened | Public results omit epoch keys; activation uses private capability plus exact readback. Browser non-extractability prevents export, not same-origin key use; XSS-resistant counter custody, WebKit signing, hidden forks, prior exposure, and physical independence remain unclaimed. |
| Public SDK/CLI package interoperability | Yes, main | Default verification-only export, explicit continuity subpath, packed-file allowlist, clean temporary install create/inspect/handoff/recover/continue, concurrent first-use identity creation, cross-process conflicting sign-once serialization, captured JSON/null-prototype journal hardening, persisted-file redaction, and PR #53 exact-head/exact-main gates | Not applicable | No | The implementation is merged, but no S5 stage receipt or public package-registry publication is claimed. CLI private keys stay in exact-schema endpoint-local authority files; crash-left locks require explicit recovery. |
| Continuity Capsule resource lifecycle | Yes, main | Canonical lineage/state/chunk binding, process-boundary verification, membership handoff, tamper tests, exact-head CI/review/merge, and exact-main readback passed for PR #53 | No | No | The implementation is merged, but no S6 integrated stage receipt is promoted. Product Capsule carries public evidence and exact resource bytes, never signing authority; it is not a confidentiality envelope. |
| Real-file A-to-B product continuity | Yes, main | Separate Node endpoint processes, clean packed consumer, and actual built-Lab Chromium run A create → B accept → A exit → B signed 2-of-3 recover → B continue; PR #53 passed governed merge and exact-main Verify/Deploy | No | No | The implementation is merged, but S5/S6 integrated receipt promotion remains open. One corrupt copy is tolerated; one copy, duplicate signed identity, stale lineage, wrong authority, and valid fork fail closed. Copy adapters remain one local administrative/failure domain. |
| Visible EN/KO real-file continuity journey | Yes, in this source revision | Native bounded file selection; relay-assisted canonical WebRTC offer/answer; file-bearing Capsule only on ordered direct DataChannel; distinct browser authorities; A→B handoff; A authority destruction; one corrupt copy rejected; exact 2-of-3 byte download; sequence 3; focused 6,016-byte and exact-ceiling 131,072-byte Chromium PASS; integrated EN/KO, premature-A-loss, visual-stability, and under-90-second UX PASS | No | No | HTTP request inspection found neither plaintext nor base64url file bytes. This source/runtime/test evidence is covered by exact-main Verify/Deploy at `a6cfb657212a0b0c20848851d86786b62b9e14f7`. The Capsule is not confidential against B. Same-host browser contexts do not prove independent hosts, administrators, networks, failure domains, or arbitrary-NAT reachability. |
| Signed bounded resource offer and network-visible consumption | Yes, main | Canonical provider offer, mutual single-use lease, role-disjoint signed Byzantine witness policy, threshold gossip announcement, minority-partition `unwitnessed` halt, duplicate idempotence, provider/witness equivocation halt, chained usage, unilateral revocation, generated limits, browser-target bundle, portable scan, and clean packed consumer | No | Yes, as the logical control contract merged by PR #56 | This proves finite signed intent and quorum-visible logical consumption only under the declared witness fault bound. It does not prove witness independence, resource possession, truthful metering, service delivery, settlement, or Sybil resistance. |
| Lease-bound storage/bandwidth/compute execution | Yes, main | Consumer challenge, dual-signed predecessor chain, exact usage binding, Merkle storage proof, payload round trip, deterministic compute, actual child-provider PID termination, new-provider/new-lease reassignment with stable workload ID, browser-target bundle, and clean packed consumer | No | Yes, within the local execution-evidence claim merged by PR #56 | This proves deterministic evidence in a local multi-process topology. It does not prove distinct hardware, account, region, credential, administrator, honest physical metering, or Sybil resistance. |
| Direct WebRTC participant transport | Yes, in this source revision | Canonical bounded manual signaling, ordered binary DataChannel, no ICE server or implicit fallback, owned publish bytes, one private combined inbound/outbound transcript, detached frozen publish/fetch/subscriber results, generated exact 512-unique-message and 8,388,608-decoded-raw-byte ceilings, duplicate non-consumption, outbound pre-send capacity with post-success commit, inbound overflow with no transcript/dedupe commit or subscriber delivery before terminal cleanup, virtual-transport exact raw-byte ceiling, unified idempotent local/remote cleanup with native peer close at most once and hostile `Error`/`Symbol.hasInstance` containment, captured WebRTC-local collection/iterator/scheduler/DataChannel/peer capabilities, captured relay artifact-kind membership, selective-`verdict` Node poison with zero send/local visibility, and actual Chromium literal count/byte cap-plus-one, remote-close, zero-local/remote verdict, and exact challenge delivery | No | No | Manual same-host ICE does not prove arbitrary Internet/NAT reachability. Signaling/STUN/TURN remain replaceable future adapters, never validity authorities. Successful local `send()` is a queueing boundary, not an end-to-end delivery receipt. The relay edge's conservative base64 estimate may reject slightly before the exact decoded-byte boundary; only the common upper ceiling/fail-closed behavior, not byte-identical accounting, is claimed. The poison evidence covers the named capabilities, not every future validator dependency. |
| Receipt-gated P2P storage placement and repair | Yes, in this source revision | Actual runtime file plus signed lease/execution evidence cross direct peers; exact active receipt gates three copies; actual provider loss degrades to two; new provider/new lease repairs; A exits; B recovers two valid copies and rejects corruption | No | No | Exact-main Verify/Deploy/public readback at `a6cfb657…` covers the source/runtime/test claim. The older plaintext regression is transport-only, and local unavailability is not global truth. |
| Confidential continuously re-proved P2P placement controller | Yes, in this source revision | Native 98,317-byte file encrypted as S4 package for B; deterministic 2-of-3 ciphertext shards; exact workload/provider/shard binding; one generation instant for contract status and proof age; expiry/revocation regressions; journal-v2 context bound to exact prior head, next generation, manifest/policy, and epoch; context-derived receipt challenge nonce; module-private branded active distinct-provider `3/3`; cumulative per-chain epoch high-waters preserved across provider replacement; exact direct successor for every previously seen chain; new-chain sequence-zero admission. The preceding Node portable-kernel gate executed 128 signed transitions with 381 replacements to generation 129, 384 distinct provider/lease/chain high-waters (`128/128/128`) and 387 receipts. The preceding mixed-runtime Chromium/Lab path executed 127 cycles from generation 2 to the identical ceiling with browser-held non-extractable provider keys producing storage results/signatures while Node orchestrated the portable journal. Both proved a signed generation-130 `3/3` candidate whose 385th total/129th shard-0 chain failed commit without changing the exact ceiling bytes; Chromium also reloaded and rejected the oldest replay. Fresh rotated-epoch v1 migration; generated document/history caps; immutable journal/context/transition files and one no-replace hard-link successor per prior; stale-writer, concurrent-winner, restart, old/unseen receipt, tamper, limit, and migration negatives in `test/confidential-journal-v2.test.mjs` and `test/confidential-controller-v2.test.mjs`; actual Chromium origin-cut recovery and exact decrypt. A historical pre-repair-hardening delta passed uninterrupted `npm test` in `7,476,222ms` through final `verify:s4`; the repair-hardened successor is covered by exact-main complete-suite Verify `33052134872/1`. | No | No | Journal, context, and transition documents are unsigned local crash-policy evidence. The hard-link CAS covers conforming writers on one filesystem; it does not prove hostile-disk integrity, discover completely hidden receipt history, or create cross-host/global consensus. The Chromium evidence is mixed-runtime and does not prove independently in-browser journal-kernel parity. These genuine cryptographic transitions run in a same-PC topology, not independent machines, networks, administrators, or failure domains; three persistent provider pages reuse already stored shard bytes. Epoch rotation bounds retained history but requires a fresh `3/3` barrier. The successor operational signer is not cryptographically bound to B's Continuity custody identity. The source/runtime/test implementation is part of exact-main release `a6cfb657…`; that promotion does not extend to topology or custody claims. Global outage truth, physical/admin independence, arbitrary NAT reachability, honest metering, and XSS-resistant signing remain HOLD. |
| Lineage-bound placement handoff and convergence | Yes, in this source revision | Generation binds organism/head/manifest/policy/latest prior/evidence/proofs/certified repair; generation `N` requires exactly `N - 1` authenticated prior placement transitions and the latest predecessor ID/head; every supplied verified Capsule tip must be represented by the candidate chain; current descriptor quorum sign-once commit gates a derived placement action plan; rogue consumers, stale priors, repeated/skipped/noncanonical/overflow numbering, and omitted current tips reject or halt; A→B transfers no private key; two fresh processes and reordered evidence converge byte-identically; 1,000 partition/heal evidence events retain one authority; independently valid same-parent siblings halt; actual Chromium A exits and B commits repaired successor | No | No | Exact-main governance is established for `a6cfb657…`; this does not create global-currentness evidence. Currentness is relative to supplied authenticated Capsules, not unseen global state. The returned action object is forgeable derived JSON, not a bearer capability; every executor must reverify the original Capsule/generation/commit and current placement/liveness evidence. Fork halt is safety, not automatic consensus. |
| Lineage-governed logical placement admission | Yes, in this source revision | Self-contained admission evidence binds a generated-bounded raw challenge, subject roles, operator root, logical domain, policy/scope, issue time, and validity; the subject and issuer sign separate domains over the same evidence ID. Trust roots bind stable authority IDs, direct sequence/predecessor links, explicit revocation, cumulative root/issuer-key history, and retired authorities; skipped rotation, silent removal, old root/key rollback, resurrection, and valid siblings halt. A custody-quorum-signed membership epoch binds the exact Capsule/head, provider exclusion, prior epoch, and deterministic roster. Alias keys collapse under one operator root; bipartite matching selects at most one observer per operator/domain; adjacent epochs require operator/domain quorum intersection. Lineage generations carry the exact referenced epoch sidecars and reject missing, duplicate, extraneous, mismatched, or history-inconsistent sidecars. Compact admitted liveness policy `/2` keeps relay-wrapped policy/challenge/certificate under the generated 65,536-byte ceiling. Focused Node/fresh-process/SDK/lineage/batch and actual Chromium paths pass; exact-main Verify `33052134872/1` covers the repair-hardened successor in the complete suite. | No | No | Dual signatures prove subject key control and the issuer's exact assertion, not issuer honesty or physical independence. Trust-root authorities, operator roots, and domain labels remain Continuity policy inputs. Current tests use a same-PC Lab and do not prove distinct devices, networks, credentials, administrators, regions, physical capacity, or absolute Sybil resistance. No economic/capacity weight is inferred. Exact-main gates pass for `a6cfb657…`; physical and administrative independence remain pending. |
| Origin-locked durable admission endpoint replay | Exact-main `a6cfb657…` source/runtime/test/release PASS | Each role-local Node client takes the canonical request, one endpoint, and only that host's generic bearer; it constructs identity/signature Requests before suspension, denies redirects and remote plaintext HTTP, bounds timeout and response streams, verifies its exact public role/identity/signature, then publishes one no-replace token-free response. A network-free coordinator combines both public responses into the canonical replay bundle. The legacy combined client remains byte-compatible but is not the strict independent-administration path. Challenge `/2` explicitly carries exact issuer and subject origins/key IDs plus their digest under the generated 512-byte ceiling. Each operator-facing service locks its own advertised origin before private-key use and can terminate native HTTPS with bounded role-local certificate/private-key bytes; incomplete, oversized, malformed, or mismatched TLS material rejects before absent authority creation. Distinct durable authority files survive restart and reproduce the exact bundle. Direct trust-store-validated observation proves distinct test certificates/public keys on the native path. Partial success recovers by exact retry; caller and post-import ambient mutation do not alter the result; origin alias, swapped response, or public bundle rehash rejects. | No | No | Current native endpoints and reverse-proxy integration fixtures are distinct processes/origins on one PC and one remote address. A signed origin declaration or distinct certificate is not external issuer honesty, separate credentials or administrators, multi-host/network/region operation, or physical failure-domain evidence. Each bearer token controls only its local HTTP(S) access and is never admission authority. |
| TLS-channel-bound live role-key deployment observation | Exact-main `a6cfb657…` source/runtime/test/release PASS | A fresh Node process restores the exact signed ceremony bundle and requires both origins to be HTTPS. Native `/2` sends a bounded ceremony/origin/role/key/nonce/time challenge to each endpoint, derives a TLS exporter with those exact bytes as context on both ends of that connection, and requires the configured role key to sign the exporter digest. The artifact records each bounded proof, exporter digest, TLS protocol, peer certificate/public-key digests, and socket address. A copied `/identity` or captured valid proof served under the same certificate on a new TLS connection rejects; missing possession-token environment never silently downgrades. Derived facts and proof signatures are recomputed on restore. The combined CLI hard-links the exact token-free observation to a no-replace journal before observer signing; with both role signers stopped and no possession tokens, a fresh process restores that journal and reproduces the byte-identical attestation. A conflicting same-plan connection fails instead of consuming another sign-once choice. | No | No | The artifact fixes `non_authority:true`, `independent_administration:"unproven"`, and `independent_failure_domains:"unproven"`. Offline restore proves canonical/signature integrity, not that the historical network observation occurred. A separately authorized possession token grants only the bounded proof route, not admission signing. Explicit `legacy-identity-only` `/1` remains for TLS-terminator compatibility and is not role-key-possession evidence. Journal durability is local-directory/no-replace scoped, not power-loss proof. Native and proxy fixtures still share one PC and remote address; CA/account custody, administrator separation, networks, regions, and physical failure domains remain external evidence. |
| Membership-bound observer-attested deployment view | Exact-main `a6cfb657…` complete-suite/CI/release PASS | Plan `/1` precommits one ceremony, logical window/timeout, and sorted 2–8 observer roster. Every durable key signs one ceremony-scoped acceptance `/1`; the complete set creates activation `/1`. Membership binding `/2` embeds that activation, exact ceremony, selected custody-quorum epoch, sorted candidate epoch IDs, and a candidate-view commitment. Creation owns the supplied candidate bytes, runs current-Capsule convergence, and rejects a missing prior/current epoch, sibling fork, unsafe history/reconfiguration, cycle, or extraneous candidate. The selected epoch must contain the exact ceremony subject and complete plan observer membership; identities and declared administration/failure-domain digests must match admitted members with distinct roots/domains. Restore is historical; explicit verification reruns the same candidate view. Attestation `/5` embeds the binding, view ID, and observation. Each durable observer has one plan-scoped attestation choice: exact journal-backed retry is idempotent, while another view, observation, or instant under that plan halts; epoch rotation requires a fresh ceremony and plan. The combined CLI verifies the complete supplied view before network access, probes with the admitted assignment, journals the exact observation before signer use, and only then signs. Compact attestation-view `/1` commits key-sorted sidecar IDs plus the derived all-roster summary without nesting signatures; restore is self-hash-only and the read-only verifier must reproduce exact bytes from every sidecar. Current-Capsule custodians now approve one request-bound epoch independently with existing durable authorities; no free-form epoch body or coordinator custody key is accepted. Each observer enters membership through its own policy-locked issuer/subject service ceremony using the same key that later accepts and attests, removing the raw private-authority fixture bypass. Output collision, non-custodian, insufficient quorum, mixed request, sign-once conflict, reversed approval order, offline journal recovery, exact retry, and prior-epoch chaining are covered. The current native `/2` deployment/public-chain path passes focused `1/1` in `109,399.7366ms`; async security audit passes `22` direct / `145` discovered. The repair-hardened successor is covered by exact-main complete-suite Verify `33052134872/1`, promoted artifact `9643090168`, and Deploy `33064417517/1`. | Policy-scoped membership and supplied-view convergence only | No | The binding removes raw disjoint unadmitted rosters, same-roster multi-plan selection, same-plan multi-view signing, and known sibling/incomplete candidate views from the conforming flow. The manifest is an unsigned self-hash until exact sidecars are explicitly reverified. Neither artifact can prove that a valid candidate omitted from every observer inventory does not exist, nor issuer honesty, globally unique membership, trusted wall-clock time, administrator identity, accounts, hosts, networks, regions, power, or physical failure domains. Authorities and native signers still share one PC; explicit proxy `/1` is compatibility evidence only. Offline signing attributes bytes; only the combined path orders current candidate-view verification, probe, journal publication, and signature. |
| Complete public pilot-chain replay | Exact-main `a6cfb657…` complete-suite/CI/release PASS | A canonical public-root-confined index names every ceremony trust root, subject identity, request, and bundle; every chronological membership request, current-custodian approval, and finalized epoch; the separate plan, acceptances, activation, current membership binding, attestations, and compact view. Creation rederives the entire chain and emits a deterministic digest/ID-only receipt. Independent verification recreates exact bytes from every sidecar; offline restore is explicitly `public_chain_verified:false`. Unordered set permutations are byte-identical; missing history, swapped ceremony input, self-rehashed receipt substitution, source-commit mismatch, path escape, and output reuse reject. | Public protocol consistency under supplied complete sidecars | No | The receipt alone records the expected commit as `recorded-only`; external exact-main CI/release supplies the separate clean-source evidence for `a6cfb657…`. It also cannot prove hidden-inventory completeness, issuer or clock honesty, administrators, credentials, accounts, hosts, networks, regions, power, physical failure domains, or Sybil resistance. |
| Role-key-attributed pilot source artifacts | Exact-main `a6cfb657…` complete-suite/CI/release PASS | The conforming role-local CLI checks exact Git `HEAD` plus a clean tracked/untracked/submodule worktree before the existing durable role authority signs the exact source commit, artifact digest/ID/kind, and role. The aggregate replays the public pilot chain and requires exactly one matching receipt for every issuer/subject ceremony response, current-custodian approval, observer plan acceptance, and observer deployment attestation. The current fixture binds 12 artifacts across 7 role keys; exact retry and reversed receipt order are byte-identical, while dirty checkout, wrong HEAD, modified signature, and a missing receipt reject. | `role-key-attested-artifacts` under the conforming CLI | No | A role signature attributes the clean-checkout statement but does not prove an honest operator used that CLI or that unsigned coordinator artifacts were executed by the named source. Exact-main CI/release covers the implementation and fixture tests at `a6cfb657…`; hidden-inventory completeness, administrator/account/host/network/physical independence, and Sybil resistance remain external. |
| Coordinator-non-authority pilot source verdict | Exact-main `a6cfb657…` complete-suite/CI/release PASS | The verdict first fully recreates the public chain and role-source aggregate, then inventories every unsigned protocol artifact by exact digest. Ceremony roots/identities/requests, membership requests, and the deployment plan must be endorsed by later participant signatures; ceremony bundles, finalized epochs, activation, current membership binding, attestation view, and pilot evidence must be deterministically replayed or revalidated. The current fixture closes 21 unsigned artifacts (12 participant-endorsed, 9 deterministically replayed), 12 role-source artifacts, and one authenticated Capsule for a 34-artifact verdict. Restore leaves all three sidecar-verification flags false; full verification recreates exact verdict bytes and sets them true. | `coordinator_protocol_authority:"not-required-for-verification"` | No | Coordinator execution remains `unproven`; the verdict removes coordinator trust from verification but does not attest which executable created unsigned bytes. Exact-main CI/release covers the implementation and fixture tests at `a6cfb657…`; public inventory completeness, issuer/clock honesty, administrator/account/host/network/physical independence, and Sybil resistance remain external. |
| All-role-key participant inventory closure | Exact-main `a6cfb657…` complete-suite/CI/release PASS | Every unique role key derived from the fully verified execution-receipt set signs the exact source-verdict ID/digest, pilot evidence, source commit, and deployment-plan ID. Each conforming durable authority has one plan-keyed sign-once choice: exact retry is byte-identical, while another self-hashed verdict for that plan halts. The aggregate recreates the entire source verdict, requires exactly one ratification from all 7/7 current keys, canonicalizes their role sets, and binds the plan back to pilot evidence. A 6/7 set produces no output; restore is sidecar-unverified and full replay reports `all-role-keys-ratified`. | Participant-ratified final inventory for one deployment plan | No | This prevents two fully closed verdicts for the same plan when at least one shared honest role key uses the conforming durable journal. It cannot discover artifacts hidden from every honest participant, prevent copied keys with separate journals, or prove global inventory completeness, issuer/clock honesty, administrator/account/host/network/physical independence, or Sybil resistance. |
| Lease-bound quorum liveness, sampled possession, and repair certificate | Exact-main `a6cfb657…` source/runtime/test/release PASS | Provider-signed compatibility policy `/1` binds exact offer/lease/provider/consumer, offer-roster digest, lineage/manifest/workload/shard/next sequence, exact 5,000 ms window, and sampled-storage response profile. Repair-authoritative admitted policy `/2` additionally references the exact custody-signed membership epoch and deterministic selection digest without embedding the sidecar. Consumer signs challenge `/2`; provider-only response `/2` proves the nonce-selected leaf/path against the exact storage root without a fresh consumer receipt; 3-of-4 observations form certificate `/2`; response plus certificate halts; pointer/legacy `/1` remains parseable but has no lineage repair authority; focused Node, fresh-process, lineage, SDK/packed, actual Chromium stored-byte/provider-key paths, and connected Chromium batch reconciliation pass | No | No | One sampled leaf/path is not every-byte or continuous custody, provider death, breach, lease termination, penalty, settlement, honest timers, externally proven independent actors/domains, or Sybil resistance. Connected reconciliation is one same-host manually signaled DataChannel pair, not background gossip, arbitrary NAT reachability, or physical-domain evidence. |
| Independent host/relay/credential-domain survival | Process-isolated model only | Three HTTP CAS processes survive one termination and disk restart | No | No | Real S7 promotion still requires distinct provider, host, administrator, credential domains, 100 trials, and seven-day burn-in. |
| Adversarial 2-of-3 Capsule custody | Yes, this source tree | Current-custodian-signed copy/provider identities recover with one lost/corrupt copy; duplicate identity, valid fork, and below-quorum sets reject | No | No | Signed logical identity prevents double-counting but does not prove an independent physical provider, private signing-key custody, or Byzantine resolution. |
| Byzantine key-threshold safety | Yes, in this source revision | Declared `n/f/q` bounds, role-disjoint offer-rostered keys, threshold certificate, and deterministic conflict halt pass | No | No | This is not Sybil resistance: one operator may control several valid keys. Forks halt automatic progress; no winner is selected. |
| Global death certificate or proof all copies are gone | No | No | No | No | Mortality remains observer-domain-relative under explicit assumptions. |
| Firefox durable participant parity | Yes, main | Portable, S2 restart/loss/repair, and S4 counter/rotation actual-engine gates passed on `12e90e6…` | No | No | The implementation is covered by exact-main release `a6cfb657…`; browser capability limits remain profile-specific. |
| WebKit durable participant parity | Capability-routed main | Windows rejects Ed25519; Ubuntu creates a key but fails the protocol-ceiling/full-quorum signing envelope, so both measured runtimes are verifier-only | No | No | Engine name or key generation never grants authority; full custody requires the exact bounded sign/verify probe and no raw-key fallback is allowed. |
| GPT/model authority | Intentionally absent | Not applicable | Not applicable | Not claimed | Optional model output cannot sign, select a head, or change a verdict. |
| Verify-gated content-addressed production release | Yes, promoted release `a6cfb657…` | Exact-main Verify `33052134872/1`, artifact `9643090168`, Deploy `33064417517/1`, and byte-stable public readback PASS | Not applicable | Yes | Deploy has no direct push/manual trigger and consumed only the successful same-run exact-SHA artifact after current-main and receipt validation. The promotion is immutable-release scoped; later source changes require fresh gates. |

## Change rule

Any implementation, documentation, website, package, or deployment statement that
changes a row must update this matrix and its exact evidence in the same reviewed
change. Unavailable evidence is recorded as **No** or **HOLD**, never inferred from
silence or a healthy endpoint.
